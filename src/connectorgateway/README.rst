Microsoft Azure CLI Connector Gateway Extension
================================================

This package is a preview Azure CLI extension for managing Microsoft.Web Connector Gateway resources.

It includes commands for Connector Gateways, available connectors, connections, connection access policies, MCP server configs, and trigger configs.