Microsoft Azure CLI Connector Namespace Extension
=================================================

This package is a preview Azure CLI extension for managing Microsoft.Web Connector Namespace resources.

It includes commands for Connector Namespaces, available connectors, connections, connection access policies, MCP server configs, and trigger configs.